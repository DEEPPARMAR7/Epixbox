# Allow peer dependency conflicts (required for react-beautiful-dnd compatibility with React 19)
# This is a calculated risk: the packages work correctly despite the version mismatches
legacy-peer-deps=true

